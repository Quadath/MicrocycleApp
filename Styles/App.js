import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    header_text: {
        fontSize: 30
    },
    new_exercise_block: {
        flexDirection: 'row'
    }
});


export default styles;